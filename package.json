{
  "name": "claude-plugins-installer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive installer that enables the matlomax Claude Code plugins in a repo. The plugins carry their own runtimes (worklog is a separate Go binary); this dependency is the installer's alone.",
  "bin": {
    "claude-plugins-install": "install.mjs"
  },
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "@clack/prompts": "1.7.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
